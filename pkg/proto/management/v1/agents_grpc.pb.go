// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v0.14.1-v0.16.6-bufbuild-protocompile-easyp
// source: proto/management/v1/agents.proto

package managementv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	AgentsAPI_Pty_FullMethodName      = "/graphene.management.v1.AgentsAPI/Pty"
	AgentsAPI_PtyInput_FullMethodName = "/graphene.management.v1.AgentsAPI/PtyInput"
)

// AgentsAPIClient is the client API for AgentsAPI service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// AgentsAPI is the operator's hand on a MACHINE: an interactive shell
// on the box whose agent is connected, over the door — no ssh keys, no
// direct reachability, the same token and audit as every other verb.
//
// A browser cannot hold a bidirectional stream, so the shell is a
// half-duplex pair: Pty opens the session and streams its output;
// PtyInput lands keystrokes, resizes and the explicit close as unary
// calls addressed by the session id the first chunk announced. The
// session is mortal by nature: it dies with the stream, the agent's
// disconnect, or the shell's own exit — nothing reconnects.
type AgentsAPIClient interface {
	// Pty opens a shell on the machine and streams everything it says.
	// The FIRST chunk is always Opened with the session id; the stream
	// ends when the shell dies. Closing the stream closes the shell.
	Pty(ctx context.Context, in *PtyRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[PtyChunk], error)
	// PtyInput writes into a live session: keystrokes, a resize, or the
	// explicit close. One frame per call — input is bursty and small,
	// and unary is what a browser can send.
	PtyInput(ctx context.Context, in *PtyInputRequest, opts ...grpc.CallOption) (*PtyInputResponse, error)
}

type agentsAPIClient struct {
	cc grpc.ClientConnInterface
}

func NewAgentsAPIClient(cc grpc.ClientConnInterface) AgentsAPIClient {
	return &agentsAPIClient{cc}
}

func (c *agentsAPIClient) Pty(ctx context.Context, in *PtyRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[PtyChunk], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &AgentsAPI_ServiceDesc.Streams[0], AgentsAPI_Pty_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[PtyRequest, PtyChunk]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AgentsAPI_PtyClient = grpc.ServerStreamingClient[PtyChunk]

func (c *agentsAPIClient) PtyInput(ctx context.Context, in *PtyInputRequest, opts ...grpc.CallOption) (*PtyInputResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(PtyInputResponse)
	err := c.cc.Invoke(ctx, AgentsAPI_PtyInput_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// AgentsAPIServer is the server API for AgentsAPI service.
// All implementations must embed UnimplementedAgentsAPIServer
// for forward compatibility.
//
// AgentsAPI is the operator's hand on a MACHINE: an interactive shell
// on the box whose agent is connected, over the door — no ssh keys, no
// direct reachability, the same token and audit as every other verb.
//
// A browser cannot hold a bidirectional stream, so the shell is a
// half-duplex pair: Pty opens the session and streams its output;
// PtyInput lands keystrokes, resizes and the explicit close as unary
// calls addressed by the session id the first chunk announced. The
// session is mortal by nature: it dies with the stream, the agent's
// disconnect, or the shell's own exit — nothing reconnects.
type AgentsAPIServer interface {
	// Pty opens a shell on the machine and streams everything it says.
	// The FIRST chunk is always Opened with the session id; the stream
	// ends when the shell dies. Closing the stream closes the shell.
	Pty(*PtyRequest, grpc.ServerStreamingServer[PtyChunk]) error
	// PtyInput writes into a live session: keystrokes, a resize, or the
	// explicit close. One frame per call — input is bursty and small,
	// and unary is what a browser can send.
	PtyInput(context.Context, *PtyInputRequest) (*PtyInputResponse, error)
	mustEmbedUnimplementedAgentsAPIServer()
}

// UnimplementedAgentsAPIServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAgentsAPIServer struct{}

func (UnimplementedAgentsAPIServer) Pty(*PtyRequest, grpc.ServerStreamingServer[PtyChunk]) error {
	return status.Error(codes.Unimplemented, "method Pty not implemented")
}
func (UnimplementedAgentsAPIServer) PtyInput(context.Context, *PtyInputRequest) (*PtyInputResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method PtyInput not implemented")
}
func (UnimplementedAgentsAPIServer) mustEmbedUnimplementedAgentsAPIServer() {}
func (UnimplementedAgentsAPIServer) testEmbeddedByValue()                   {}

// UnsafeAgentsAPIServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AgentsAPIServer will
// result in compilation errors.
type UnsafeAgentsAPIServer interface {
	mustEmbedUnimplementedAgentsAPIServer()
}

func RegisterAgentsAPIServer(s grpc.ServiceRegistrar, srv AgentsAPIServer) {
	// If the following call panics, it indicates UnimplementedAgentsAPIServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AgentsAPI_ServiceDesc, srv)
}

func _AgentsAPI_Pty_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(PtyRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(AgentsAPIServer).Pty(m, &grpc.GenericServerStream[PtyRequest, PtyChunk]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AgentsAPI_PtyServer = grpc.ServerStreamingServer[PtyChunk]

func _AgentsAPI_PtyInput_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(PtyInputRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AgentsAPIServer).PtyInput(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AgentsAPI_PtyInput_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AgentsAPIServer).PtyInput(ctx, req.(*PtyInputRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// AgentsAPI_ServiceDesc is the grpc.ServiceDesc for AgentsAPI service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AgentsAPI_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "graphene.management.v1.AgentsAPI",
	HandlerType: (*AgentsAPIServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "PtyInput",
			Handler:    _AgentsAPI_PtyInput_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Pty",
			Handler:       _AgentsAPI_Pty_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "proto/management/v1/agents.proto",
}
