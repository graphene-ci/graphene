# signoz-standalone v0.0.4 ships units with User=clickhouse but no such
# user in the image — declare it so systemd-sysusers creates it at boot
# (drop when the upstream image fixes this).
u clickhouse - "ClickHouse" /var/lib/clickhouse
