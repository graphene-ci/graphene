# Основа образов, прибитая дайджестом.
#
# По умолчанию ko берёт cgr.dev/chainguard/static:latest — и `latest`, и
# требует авторизации, то есть нарушает наше правило и не собирается на
# чистой машине. distroless тянется без учётки; дайджест вместо тега —
# по тому же правилу, что и везде: тег переставляют.
defaultBaseImage: gcr.io/distroless/static-debian12@sha256:1b7b9f0f0e0a1d2155f531db587cc48ec26aaf97ab64364225f5bf18a054e66a
