# План: от пустого репозитория до работающей v0

**Цель:** система, в которой перф-случай — поднять машины, поставить
постгрес, прогнать тест, снять метрики, свернуть — описан одной программой
на Go, выполняется внутри системы и переживает падение чего угодно.

**Раскладка:** k3s держит записи и владение, Temporal держит ход дела,
Crossplane даёт ресурсы, наш оператор сшивает первое со вторым, агент
выполняет на машинах. Обоснования — в `FORM.md`, «зачем» — в `VISION.md`.

**Из v0 выброшено:** UI, TUI, языки кроме Go, генерация типов из CRD, кэш
по содержимому.

## Постоянные ограничения

Действуют в каждой задаче, повторять не буду.

- Go; никаких `replace` в `go.mod` — зависимости берутся с GitHub.
- **Один прибитый тулчейн** (`GO_VERSION` в `Makefile`) на сборку, тесты и
  инструменты. Не вкусовщина: kubernetes 0.36 требует 1.26, а линтер,
  собранный старше, не сообщает об этом — он падает в панике посреди
  анализа.
- Обязательный набор репозитория: `README.md`, `AGENTS.md`, `Makefile` с
  целями `configure` и `help`, каталог `bin/` с прибитыми версиями
  инструментов. Ни одного `latest`, ни одной глобальной установки.
- Коммиты — Conventional Commits, без приписок об ИИ.
- `make test` и `make lint` зелёные перед каждым push: 0 падений, 0 issues.
  Где линтер неправ — исключение в `.golangci.yaml` **с записанной
  причиной**, никогда голый `//nolint`.
- Документация — текущее состояние, а не журнал изменений. Взятая веха
  сворачивается до своего доказательства и того, что из неё стало
  правилом; список её задач — архив, а не план.

## Раскладка репозитория

`github.com/graphene-ci/graphene`, один модуль. Второй (`/sdk`) выделяется
тогда и только тогда, когда тест на граф импортов начнёт мешать
по-настоящему.

```
api/v1/                   наши виды: структуры, deepcopy, scheme
pkg/pipeline/             SDK, который импортирует ПОЛЬЗОВАТЕЛЬ
pkg/agent/                контракт: имена activity, очереди, формы аргументов
pkg/lib/                  обёртки-полезности: пример, а не каталог
cmd/graphene/             CLI: devkit и клиент
cmd/graphene-operator/    операторы
cmd/graphene-worker/      системный воркер
cmd/graphene-agent/       агент
internal/                 всё, чего не импортируют снаружи
deploy/                   манифесты установки и локальное окружение
examples/                 примеры, они же первые настоящие пользователи
test/e2e/                 сквозные проверки за тегом сборки
```

**`pkg/pipeline` импортирует пользователь**, поэтому он не имеет права
тянуть controller-runtime, клиент кубера и облачные SDK. Это проверяет
`pkg/pipeline/imports_test.go` — по модулям, а не по пакетам: пакетов у
Temporal и apimachinery сотни и они меняются от патча к патчу.

## Вехи

Каждая названа своим **доказательством**, а не списком работ. Веха
считается взятой, когда доказательство воспроизводится на чистой машине.

| | Веха | Доказательство | |
|---|---|---|---|
| **M0** | Репозиторий, окружение, скелет devkit | `make configure && make up && make test` на чистой машине | **взята** |
| **M1** | Сшивка: запись → воркфлоу → запись | `make e2e`: прогон доехал, запись создана ровно одна; оператор убит посреди — доехало | **взята** |
| **M2** | Агент | `make e2e`: шаг доехал до контейнера-машины, машина завела себя сама | **взята** |
| **M3** | Облако | Пайплайн поднял настоящую ВМ в Yandex Cloud и выполнил на ней команду; агент приехал и на машину, которая уже была | **взята** |
| **M4** | Владение и снос | `make e2e`: отмена сносит созданное, `Keep` оставляет стенд с концом, удаление прогона уносит записи; на облаке — удаление прогона уносит ВМ | **взята** |
| **M5** | Аренда и выбор | Два прогона одновременно не взяли одну машину | |
| **M6** | Артефакты и наблюдаемость | Отчёт в хранилище; «почему стояло восемь минут» — одна трасса | |
| **M7** | Полное API | Весь путь проходится CLI, `kubectl` не нужен ни разу | |
| **M8** | Перф-случай | Настоящий прогон, которым пользуется человек не из нашей команды | |

Подробно расписана только очередная веха. Следующая детализируется, когда
доехала предыдущая: расписывать M5 сегодня — сочинять, потому что
половина предположений умрёт о первую проверку. M1 это подтвердил —
четыре из его ошибок не поймал бы ни один модульный тест.

---

# M0 и M1 — взяты

Что из них стало правилом и больше не обсуждается.

**Сборка и установка.**
`make up` поднимает k3s, Temporal и Crossplane; `make install` ставит наш
управляющий слой; `make e2e` проходит всё насквозь. Реестр образов живёт
внутри `k3d`: **ревизия обязана держать дайджест, а дайджест бывает только
у образа, который куда-то положили**. Зеркало в containerd делает так, что
`localhost:5555` значит одно и то же с машины и изнутри кластера, и
`/etc/hosts` трогать не надо.

**Виды** — `Pipeline`, `PipelineRevision`, `Run`, `Probe` в группе
`graphene-ci.dev/v1`. `Run` не исчезает по завершении: он меняет фазу, и
запись есть история. Предел размера параметров и результата проверяется в
Go, а не схемой CRD: схема считает ключи, а не байты произвольного JSON.

**Идемпотентность держится на именах, а не на блокировках.**
Идентификатор воркфлоу равен имени записи `Run`. Имя записи в кластере —
чистая функция от прогона и памятки (`agent.ObjectName`). Второй проход
любого рода сталкивается с первым вместо того, чтобы завести второе.

**Очередь у каждой ревизии своя**, и оператор поднимает по воркеру на
ревизию. Отсюда версионирование даром: старые прогоны дотекают на старых
воркерах, `patched()` не нужен.

**Наши activity выполняются на своей очереди** (`agent.SystemQueue`).
По умолчанию Temporal шлёт activity в очередь заказавшего воркфлоу — и
тогда каждый образ пайплайна нёс бы клиент кубера и право писать записи.

**Ожидание готовности — сигналом**, воркфлоу спит в истории. **Ход дела
оператор опрашивает**: запись перестаёт меняться в момент старта воркфлоу,
и разбудить контроллер больше нечему.

**Вид объекта SDK узнать неоткуда.** Проверено: сгенерированный тип несёт
пустой `TypeMeta`, глобального реестра схем в Go нет, достать переменную
пакета рефлексией нельзя. Поэтому `pipeline.Serve(fn, pipeline.Scheme(…))`
— одна строка в `main` пользователя, а `Apply` без неё отказывается и
называет ту строку, которую надо дописать. Честный выход появится с
генерацией типов, после v0.

---

# M2 — взята

**Доказательство:** `make e2e` — пайплайн называет машину, скрипт
установки поднимает агента в обычном alpine-контейнере, шаг доезжает и
возвращает код возврата и вывод, `Machine` появляется сама с фактами.

Что стало правилом.

**Агент — воркер Temporal на очереди своей установки.** Входящего порта у
него нет и не будет. Очередь и есть ожидание: шаг, поставленный в очередь
несуществующего пока агента, ждёт в ней — проверка нарочно поднимает
машину ПОСЛЕ прогона, чтобы это было доказано, а не подразумевалось.

**Очередь принадлежит установке, не машине.** Переустановка даёт новую
очередь, и шаг не может доехать до зомби прежней.

**У агента нет доступа к кластеру и не будет.** Он регистрируется
воркфлоу через системную очередь: воркер не может поставить activity сам,
а токен, позволяющий машине писать записи, был бы ключом от кластера,
розданным вместе с каждой установкой.

**По таймауту умирает дерево процессов, а не группа.** Измерено: shell
уводит фоновое задание в собственную группу, и убийство по группе
оставляет его жить. Правильный механизм — cgroup на шаг, и он придёт
даром, когда агент станет юнитом systemd.

**Ненулевой код возврата — ответ, а не отказ.** Отказ — это когда мы не
смогли выяснить.

**Два срока живости независимы.** Аренда протухла — машина не принимает
новое; идущий шаг это не трогает, за него отвечают таймауты Temporal.

**Бинарь агента едет в образе оператора как ko-данные.** «Оператор
установлен» и «агента нужной версии можно поставить» — один факт.

**Не сделано и перенесено в M3: задача 2.7, `MachineIntent`** — приход на
существующую машину по ssh. Доказательство вехи от неё не зависело, а
ssh — это отдельный разговор про ключи и про то, кто до кого достаёт.
Обещание из `VISION` остаётся в силе и закрывается в M3 вместе с облаком.

---

# M3 — взята

**Доказательство:** пайплайн поднял настоящую ВМ в Yandex Cloud через
Crossplane, агент приехал в неё через `user-data` и вернул с неё
`code=0` и вывод команды. Отдельно: `MachineIntent` поставил агента по
ssh на машину, которая уже была, — и она завела себя сама той же
записью `Machine`, что и облачная.

Что стало правилом.

**Типы провайдера — обычные типы Go.** Пример с облаком не содержит ни
одного `if` по облаку и ни одной строки шаблона: `user-data` это поле
провайдера, в которое кладётся то, что породил наш же SDK.

**Пайплайн — отдельный модуль.** Не удобство, а необходимость: провайдер
прибит к предрелизной `crossplane-runtime`, собирающейся только с
controller-runtime ≤0.23, а оператору нужен 0.24. SDK опущен до k8s 0.35,
потому что провайдеры живут на 0.33–0.35, а оператор остался на 0.36 —
ровно ради этого раскол и делался. `ko` и `go run` работают из каталога
пайплайна: у него свой `go.mod`, и наш ему не указ.

**Требования ревизии — один список, два применения.** По нему прогону
отказывают до старта, если вида нет в кластере, и по нему же заводится
наблюдение за готовностью. Виды machinery отсеиваются **по пакету типа**:
`metav1.AddToGroupVersion` кладёт свои `CreateOptions` и `WatchEvent` в
нашу же группу и версию, и по имени они неотличимы от наших.

**Готовность любого вида** — динамические информеры, заводимые по
требованию и фильтрованные по нашей метке: чужой `Instance` в том же
кластере не наше дело.

**Провайдер v2 держит две группы**, и пространственные записи ищут учётку
в своей — `ClusterProviderConfig` в `m.jet`. Без него записи создаются, а
до облака не доходят.

**Ключ хоста проверяется, и отказаться нельзя.** Доверие при первом
подключении — это то, что делает человек за терминалом; здесь управляющий
слой открывает на той стороне корневую оболочку и кормит её скриптом с
токеном установки внутри. Без ключа хоста установка не идёт вовсе.

**`MachineIntent` не создаёт `Machine`.** Её заводит агент, когда
подключится, ровно как на облачной ВМ: одно доказательство существования,
а не два. Скрипт уезжает через stdin, а не файлом — на чужой машине не
остаётся ничего лишнего, и токен установки не лежит там, где его
прочитают.

---

# M4 — взята

**Доказательство:** `make e2e` — отмена прогона сносит созданное им,
`Keep` оставляет стенд, и стенд уносит записи, когда его сносят,
удаление записи прогона уносит записи. На настоящем облаке проверено
отдельно: `kubectl delete run` дождался исчезновения ВМ.

Что стало правилом.

**Удаление прогона значит то, что читается.** Финализатор держит запись,
пока в облаке что-то осталось: сборщик кубера снимает детей удалённого
владельца, но никому не отвечает за то, догнало ли облако. Воркфлоу
останавливается первым — идущий пайплайн создаёт вещи, и уборка под ним
была бы гонкой.

**Отмена — не убийство.** Отменённый пайплайн получает возможность
прибрать за собой; убийство оставило бы машины стоять, а запись говорила
бы, что прогон окончен. Просьба живёт в спеке записи: решения о мире —
записи, и просьба переживает того, кто её высказал.

**`Keep` — передача владения, а не отсрочка.** Отсрочка кончается сносом,
и пришедший утром человек не находит ничего. Владение переходит стенду,
у стенда есть «до», и «никогда» не предусмотрено.

**В воркфлоу нельзя блокироваться, пока разматывается паника.** Стоило
двух заходов: сначала снос уехал из паники, но остался в той же
отложенной функции, которая её поймала, — а Temporal считает разматывание
незавершённым, пока эта функция выполняется. Снос делается в обычном
потоке, после того как `defer` вернул управление.

**Машина принадлежит тому, что породило её железо.** Уточнение решения
M2: «у Machine нет владельца» верно для машины, которую построил кто-то
другой, и ложно для облачной ВМ, созданной прогоном. Агент на чьём-то
ноутбуке не принадлежит никому и не убирается никогда.

---

# M5. Аренда и выбор

**Доказательство:** два прогона, запущенные одновременно и просящие одну и
ту же машину, не получают её оба — один берёт, другой ждёт. Пайплайн
просит машину **описанием**, а не именем: «linux, есть докер, не занята
чужим», и получает ту, что подходит.

### Задача 5.1: факты становятся метками

**Файлы:** `internal/operator/machine.go`, тест рядом.

Долг из M2, названный тогда же: выбирать машины по фактам нельзя, пока
факты лежат только в статусе — `metav1.LabelSelector` смотрит в метки.

Проецирует **оператор**, в одну сторону: факты — правда, метки — их
проекция. Один писатель на поле, иначе два источника разойдутся.

Не всякий факт станет меткой: значение метки — не длиннее 63 символов и
из ограниченного набора знаков. Что не влезает, остаётся только в
статусе; терять правду ради удобства выборки нельзя.

- [ ] **Шаг 1.** Тест: `docker=27.3.1` становится меткой с нашим
  префиксом; факт с недопустимым значением не становится, но остаётся
  в статусе; метки, поставленные человеком, не трогаются.
- [ ] **Шаг 2.** Написать.

### Задача 5.2: захват машины

**Файлы:** `sdk/api/v1/claim_types.go`, `internal/worker/claim.go`,
`internal/operator/claim.go`.

Захват — это **владение с концом**, и записывается он так же, как всё
остальное владение: у машины появляется поле «кем занята», и оно
указывает на запись, у которой есть владелец и срок.

Владелец захвата — прогон. Отсюда бесплатно: прогон кончился — захват
исчез — машина свободна. `Keep` передаёт захват стенду вместе с
остальным, потому что механизм один.

Взятие — **сравнение с обменом**: пишем «занята мной» с проверкой версии
записи. Проиграл гонку — читаем заново и пробуем следующую. Никаких
блокировок и никакого планировщика: планировщик появляется, когда надо
решать, кому достанется дефицит, а нам достаточно, чтобы двое не взяли
одно.

- [ ] **Шаг 1.** Тест: два захвата одной машины — второй получает отказ;
  освобождение возвращает машину в пул.
- [ ] **Шаг 2.** Написать взятие через CAS.

### Задача 5.3: всё или ничего

**Файлы:** `internal/worker/claim.go`.

«Дай три машины» обязано быть всё-или-ничего: взять две и ждать третью —
это способ занять парк и не сделать работу. Два прогона, каждый взявший
половину нужного, стоят вечно.

Порядок захвата **фиксированный** (по имени), и это не мелочь: он и
делает взаимную блокировку невозможной без всякого арбитра.

- [ ] **Шаг 1.** Тест: не хватило машин — отпускаем взятые и ждём, а не
  держим.
- [ ] **Шаг 2.** Тест: два прогона по три машины на пяти — один взял,
  другой ждёт; захваченного пополам ни у кого нет.
- [ ] **Шаг 3.** Написать.

### Задача 5.4: выбор и отталкивание

**Файлы:** `sdk/pipeline/claim.go`, `internal/worker/claim.go`.

Пайплайн просит машину описанием:

```go
node := pipeline.Claim(run, "pg-0", pipeline.Match{
    Labels: map[string]string{"os": "linux", "docker": "27.3"},
    Tolerate: []string{"dedicated=perf"},
})
```

Селектор — `metav1.LabelSelector`, тот же, что во всём кластере: чужой
язык выборки мы не изобретаем. `taints` на машине отталкивают всё, что их
не терпит, — так человек забирает машину из общего пользования, не убирая
её из системы.

Анти-аффинность («три машины на разных хостах») **в v0 не входит**: она
нужна, когда парк большой и разнородный, а доказательство вехи от неё не
зависит. Записано, чтобы не изобреталось само.

- [ ] **Шаг 1.** Тест: выбирается подходящая, не выбирается непод-
  ходящая, taint без tolerations отталкивает.
- [ ] **Шаг 2.** Написать.

### Задача 5.5: сквозная проверка

**Файлы:** `examples/claim/`, `test/e2e/m5_test.go`.

- [ ] **Шаг 1.** Пример: пайплайн просит машину по описанию и выполняет
  на ней шаг.
- [ ] **Шаг 2.** Два прогона одновременно на одну машину — один работает,
  другой ждёт и дожидается.
- [ ] **Шаг 3.** Прогон убит посреди — машина освободилась.

---

## Что решится по дороге, а не сейчас

Записано в `FORM.md` как обязательства; ниже — на какой вехе они перестанут
быть отложенными.

| Что | Веха |
|---|---|
| `AddToScheme`: как SDK узнаёт GVK типов провайдера | **проверено в M1**: без пользователя нельзя; честный выход — с генерацией типов, после v0 |

| Убийство дерева процессов вместо cgroup на шаг | M3, вместе с агентом как юнитом systemd |
| Готовность записи любого вида, а не только своего | M3 — там появляется первый чужой вид, и динамические информеры перестают быть догадкой |
| Срок хранения `Run` | поле есть с M1; уборщик — M4 |
| **Запись `Machine` переживает своё железо.** Владельца у неё нет намеренно, каскад до неё не дотягивается, и «машина выключена на выходные» неотличимо от «машины больше нет». Для облачной ВМ признак есть — исчезла создавшая её запись; для машины по ssh его нет, а метки и taints человека терять нельзя | M4 |
| Готовность провайдера в требованиях, а не только наличие CRD | M3 |
| Тип, выбранный в рантайме, тянет оба провайдера в требования | M3 |
| Токен установки лежит в спеке и в истории Temporal | **назван в коде** (`agent.Install.Token`), закрывается после M8 |
| Права: и наши, и Temporal, который теперь смотрит наружу | M7 |
| **Владелец в `Apply` заявляется, а не проверяется**: недоверенный воркфлоу называет, от чьего имени создать запись | M7 |
| **Воркер пайплайна может подписаться на нашу системную очередь** и получать `Apply`/`Teardown`, выполняемые нашими правами | M7 |
| `pipeline.Do`: законная дверь для вычислений, без неё инвариант «работа не живёт в воркфлоу» нечем соблюсти | ближайшая веха |
| Изоляция воркера как условие CI-как-услуги: песочница, квоты, учёт | M7 |
| Оператор поднимает `Deployment` на ревизию — **отменено**: это и есть выполнение чужого кода у нас. Убрать | ближайшая веха |
| Раздача агента наружу: сейчас `127.0.0.1`, с настоящей ВМ недостижим | M3 — там появляется машина, которой нужен достижимый адрес |
| Опрос Temporal раз в 5 секунд на каждый идущий прогон | M7, вместе с эксплуатационной раскладкой |
